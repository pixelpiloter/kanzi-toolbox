# kanzi 绑定之AI解读

> 原文: https://blog.csdn.net/chen_227/article/details/160254798  
> 发布: 2026-04-17  

背景:kanzi 的绑定机制很方便,但是绑定内容多了,也很难理解业务逻辑。引入AI,让AI帮助理解代码,并检查错误,提出修改意见。

1. 架构

[IMG]

2. 准备代码

参考 kanzi3.6.10 窗口插件-美化绑定内容 

3. Claude生成代码

把代码和大模型的api给claude,让claude生成计划,然后claude按计划生成代码

```
# AI 绑定信息解读功能设计方案

## Context
当前 C# WPF 插件 &#96;UserControl1.xaml.cs&#96; 用于在 Kanzi Studio 中展示绑定表达式,通过 &#96;ClassHelper.StrConvertWPF()&#96; 解析表达式并以彩色高亮显示。用户希望增加 AI 解读功能,让 AI 理解绑定表达式的含义,解释 STEP 等函数的计算逻辑,并列出不同输入值的计算结果表格。

## 设计方案
。。。

### 2. UI 增强 (修改 &#96;UserControl1.xaml&#96; 和 &#96;UserControl1.xaml.cs&#96;)

**新增 UI 元素:**
- 每个绑定项旁边添加 "AI 解读" 按钮
- 按钮下方显示 AI 解读结果 (TextBlock 或 Expander)

。。。

### 3. System Prompt 设计

。。。

### 4. 异步交互流程
。。。
### 5. 文件修改清单
。。。

## 关键实现细节

关键实现:
- 使用 &#96;System.Text.Json&#96; 进行 JSON 序列化/反序列化
- 直接向 AI 代理服务器发送请求,无需中间层
- 解析返回的 &#96;choices[0].message.content&#96; 字段

### 按钮点击事件

## 验证方案

。。。

## 注意事项
。。。

```

4. visual studio编译生成dll给kanzi 3.9.8用

使用效果
[IMG]
[IMG]
[IMG]
[IMG]
[IMG]

[IMG]